<% provide(:title, "Frage #{@question.id}") %>

<div>
  <%= render "questions/render_full", q: @question, show_question_id: true %>
  <br/><br/>
  <%=link_to "Diese Frage jetzt beantworten", "#{main_hitme_path}#hide-options#question=#{@question.id}" %>
  <% if admin? || reviewer? %><%= link_to "Diese Frage im Admin-Modus betrachten", question_path(@question) %><% end %>
</div>
